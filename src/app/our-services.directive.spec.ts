import { OurServicesDirective } from './our-services.directive';

describe('OurServicesDirective', () => {
  it('should create an instance', () => {
    const directive = new OurServicesDirective();
    expect(directive).toBeTruthy();
  });
});
